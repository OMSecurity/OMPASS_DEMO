/*const rpServer = "https://www.example.com"; // Enter your domain*/
const rpServer = "https://www.ompass.kr:7000"; // Enter your domain
const interfaceServerUrl = "https://interface-api.ompasscloud.com";

export const RP_SERVER_URL = () => {
  return rpServer;
};

export const INTERFACE_SERVER_URL = () => {
  return interfaceServerUrl;
};
