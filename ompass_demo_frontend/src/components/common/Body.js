import React from 'react';


const Body = ({children}) => {
    return (
        <>
            <div className="wrapper">
                {children}
            </div>

        </>
    );
};

export default Body;